import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  textLoading: {
    paddingVertical: 5,
    fontSize: 15,
    fontWeight: 600,
    color: "#535353",
  },
  containerLoading: {
    textAlign: "center",
    margin: "auto",
    marginTop: 240,
  },
});
